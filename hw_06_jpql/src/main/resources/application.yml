spring:
  datasource:
    url: jdbc:h2:mem:maindb
  jpa:
#    generate-ddl: false
    generate-ddl: true
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
#  h2:
#    console:
#      path: /h2-console
#      settings:
#        web-allow-others: true